<%= form_for([@post, @comment], url: post_comments_path(@post), method: :post, remote: true) do |form| %>
    <!-- Form fields for the comment -->
    <%= form.text_area :content, placeholder: "Enter your comment here" %>
    <%= form.submit "Submit Comment" %>
  <% end %>
  