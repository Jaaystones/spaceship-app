<h1>Delete Post</h1>

<p>Are you sure you want to delete this post?</p>

<p><strong><%= @post.content %></strong></p>

<%= link_to 'Confirm', @post, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
<%= link_to 'Cancel', @post, class: 'btn btn-secondary' %>
